; Titan_InferenceCore.asm - Direct GGUF Inference (No External Server)OPTION CASEMAP:NONEincludelib kernel32.libincludelib ntdll.lib.const GGUF_MAGIC         EQU 046554747h GGUF_VERSION       EQU 3 TYPE_F32           EQU 0 TYPE_Q4_0          EQU 2 TYPE_Q2_K          EQU 14 ROPE_THETA         EQU 10000.0 ; Renamed to avoid reserved word conflicts val_half       REAL4 1.5 val_scale      REAL4 0.12345 val_epsilon    REAL4 1.0e-5 val_one        REAL4 1.0TransformerCtx STRUCT    pFileBase          QWORD ?    cbFileSize         QWORD ?    pTensorIndex       QWORD ?    Architecture       DWORD ?    nLayers            DWORD ?    nHeads             DWORD ?    nEmbed             DWORD ?    nCtx              DWORD ?TransformerCtx ENDS.codeALIGN 16; ----------------------------------------------------------------------------; Dequantize_Q2_K_Block; ----------------------------------------------------------------------------Dequantize_Q2_K_Block PROC FRAME    push rbx    push r12    push r13    push r14    push r15    sub rsp, 32    .endprolog        mov r12, rcx    mov r13, rdx    xor r15, r15    @loop:    cmp r15, 256    jae @done        mov rax, r15    shr rax, 2    movzx ebx, BYTE PTR [r12+12+rax]        mov rcx, r15    and rcx, 3    shl rcx, 1    shr ebx, cl    and ebx, 3        cvtsi2ss xmm0, ebx    ; Load via explicit DWORD PTR    movss xmm1, DWORD PTR val_half    subss xmm0, xmm1    movss xmm1, DWORD PTR val_scale    mulss xmm0, xmm1        movss [r13+r15*4], xmm0        inc r15    jmp @loop    @done:    add rsp, 32    pop r15    pop r14    pop r13    pop r12    pop rbx    retDequantize_Q2_K_Block ENDP; ----------------------------------------------------------------------------; LayerNorm; ----------------------------------------------------------------------------LayerNorm PROC FRAME    push rbx    .endprolog    mov rbx, r9        vxorps xmm0, xmm0, xmm0    mov rax, rcx    @sum_sq:    vmovss xmm1, DWORD PTR [rax]    vmulss xmm1, xmm1, xmm1    vaddss xmm0, xmm0, xmm1    add rax, 4    dec rbx    jnz @sum_sq        cvtsi2ss xmm1, r9    vdivss xmm0, xmm0, xmm1        movss xmm2, DWORD PTR val_epsilon    vaddss xmm0, xmm0, xmm2        vsqrtss xmm0, xmm0, xmm0    movss xmm2, DWORD PTR val_one    vdivss xmm0, xmm2, xmm0        pop rbx    retLayerNorm ENDP; ----------------------------------------------------------------------------; Titan_RunInference; ----------------------------------------------------------------------------PUBLIC Titan_RunInferenceTitan_RunInference PROC FRAME    push rbp    mov rbp, rsp    push rbx    push r12    push r13    .endprolog        mov rbx, rcx        pop r13    pop r12    pop rbx    pop rbp    retTitan_RunInference ENDPPUBLIC GGUF_LoadFileGGUF_LoadFile PROC FRAME    .endprolog    xor eax, eax    retGGUF_LoadFile ENDPPUBLIC MatMul_Q2_KMatMul_Q2_K PROC FRAME    .endprolog    retMatMul_Q2_K ENDPEND

import { Observable, operate } from '../Observable.js';
export function skipWhile(predicate) {
    return (source) => new Observable((destination) => {
        let taking = false;
        let index = 0;
        source.subscribe(operate({ destination, next: (value) => (taking || (taking = !predicate(value, index++))) && destination.next(value) }));
    });
}//# sourceMappingURL=http://go/sourcemap/sourcemaps/dc8361355d709f306d5159635a677a571b277bc0/node_modules/rxjs/dist/esm/internal/operators/skipWhile.js.map
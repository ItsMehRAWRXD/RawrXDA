;  =====================================================================
; LANGUAGE SCAFFOLDERS - EXTENDED STUBS
; Minimal implementations for 12 extended languages
; =====================================================================

PUBLIC ScaffoldJava, ScaffoldCSharp, ScaffoldSwift, ScaffoldKotlin
PUBLIC ScaffoldRuby, ScaffoldPHP, ScaffoldPerl, ScaffoldLua
PUBLIC ScaffoldElixir, ScaffoldHaskell, ScaffoldOCaml, ScaffoldScala

.code

ScaffoldJava PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldJava ENDP

ScaffoldCSharp PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldCSharp ENDP

ScaffoldSwift PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldSwift ENDP

ScaffoldKotlin PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldKotlin ENDP

ScaffoldRuby PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldRuby ENDP

ScaffoldPHP PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldPHP ENDP

ScaffoldPerl PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldPerl ENDP

ScaffoldLua PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldLua ENDP

ScaffoldElixir PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldElixir ENDP

ScaffoldHaskell PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldHaskell ENDP

ScaffoldOCaml PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldOCaml ENDP

ScaffoldScala PROC
    ; RCX = project path, RDX = project name
    ret
ScaffoldScala ENDP

END

#pragma once

// Forward to the canonical public header to avoid duplicate Qt MOC definitions.
// The implementation lives in `include/tokenizer_selector.h`.
#include "../../include/tokenizer_selector.h"

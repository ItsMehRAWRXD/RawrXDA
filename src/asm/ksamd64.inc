; =============================================================================
; ksamd64.inc — Minimal x64 MASM compatibility shim
; =============================================================================
; For ASM that expects "ksamd64.inc" (Windows SDK style). RawrXD uses
; RawrXD_Common.inc for shared constants and EXTERNs.
; =============================================================================
option casemap:none
INCLUDE RawrXD_Common.inc

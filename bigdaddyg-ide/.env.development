# Dev server + HMR: bind to IPv4 loopback so Electron and webpack-dev-server agree (reduces flaky
# net::ERR_INTERNET_DISCONNECTED / hot-update.json failures on some Windows setups).
HOST=127.0.0.1
WDS_SOCKET_HOST=127.0.0.1
WDS_SOCKET_PORT=3000

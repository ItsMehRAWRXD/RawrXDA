cmake_minimum_required(VERSION 3.20)
project(RawrXD-AgenticIDE VERSION 1.0.0)

set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Enable MASM assembly language
set(CMAKE_ASM_MASM_COMPILER "C:/VS2022Enterprise/VC/Tools/MSVC/14.50.35717/bin/Hostx64/x64/ml64.exe")
enable_language(ASM_MASM)

add_definitions(-DNOMINMAX -D_WIN32_WINNT=0x0A00)

# Includes
include_directories(
    ${CMAKE_SOURCE_DIR}/include
    ${CMAKE_SOURCE_DIR}/src
    ${CMAKE_SOURCE_DIR}/src/agent
    "C:/VulkanSDK/1.4.328.1/Include"
)

link_directories(
    "C:/VulkanSDK/1.4.328.1/Lib"
)

# Source files
set(SOURCES
    src/agentic_ide_main.cpp
    src/agentic_ide.cpp
    src/agentic_engine.cpp
    src/zero_day_agentic_engine.cpp
    src/universal_model_router.cpp
    src/cloud_api_client.cpp
    src/cpu_inference_engine.cpp
    src/gguf_loader.cpp
    src/gguf_vocab_resolver.cpp
    src/codec.cpp
    src/tool_registry.cpp
    src/plan_orchestrator.cpp
    src/terminal_pool.cpp
    src/lsp_client.cpp
    src/action_executor.cpp
    src/multi_tab_editor.cpp
    src/chat_interface.cpp
    src/RawrXD_SignalSlot.cpp
    src/RawrXD_Window.cpp
    src/RawrXD_Application.cpp
    src/RawrXD_Renderer_D2D.cpp
    src/RawrXD_TextBuffer.cpp
    src/RawrXD_UndoStack.cpp
    src/RawrXD_Editor.cpp
    src/RawrXD_Lexer.cpp
    src/RawrXD_StyleManager.cpp
    src/RawrXD_Lexer_MASM.cpp
    
    # Assembly Interconnect Components
    src/masm/interconnect/RawrXD_System_Primitives.asm
    src/masm/interconnect/RawrXD_GPU_Memory.asm
    src/masm/interconnect/RawrXD_Inference_Engine.asm
    src/masm/interconnect/RawrXD_Complete_Interconnect.asm
    src/masm/interconnect/RawrXD_RingBuffer_Consumer.asm
    src/masm/interconnect/RawrXD_HTTP_Router.asm
    src/masm/interconnect/RawrXD_Model_StateMachine.asm
    src/masm/interconnect/RawrXD_Swarm_Orchestrator.asm
    src/masm/interconnect/RawrXD_Agentic_Router.asm
    src/masm/interconnect/RawrXD_Streaming_Formatter.asm
    src/masm/interconnect/RawrXD_JSON_Parser.asm
)

# Add headers to sources for IDE visibility (optional but good)
file(GLOB_RECURSE HEADERS 
    "src/*.h"
    "src/*.hpp"
)

# Executable
add_executable(RawrXD-AgenticIDE ${SOURCES} ${HEADERS})

# Windows Libraries
if(WIN32)
    target_link_libraries(RawrXD-AgenticIDE PRIVATE
        User32
        Advapi32
        Shell32
        Shlwapi
        d2d1
        dwrite
        windowscodecs
        ole32
        oleaut32
        comctl32
        rpcrt4
        winhttp
    )
endif()

message(STATUS "Configured RawrXD-AgenticIDE for build.")

/**
 * Model registry tests — C++20 stub (Qt-free).
 * Original used QTest, QSqlDatabase, QSqlQuery, QStandardPaths, QDir, QDateTime,
 * QTableWidget, QPushButton, QLineEdit, QComboBox, QMessageBox, QFileInfo, QTemporaryDir, QSignalSpy.
 */

#include <cstdio>
#include <cstdlib>

int main(int, char**) {
    std::fprintf(stderr, "[model_registry_test] C++20 stub. Model registry in src/core + include/model_registry.h (no Qt).\n");
    return 0;
}

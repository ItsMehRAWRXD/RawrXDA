/**
 * Semantic diff analyzer test — C++20 stub (Qt-free).
 * Original used QObject, QJsonArray, QJsonObject, QJsonDocument, QCryptographicHash, QTemporaryDir, QFile, QDir.
 */

#include <cstdio>
#include <cstdlib>

int main(int, char**) {
    std::fprintf(stderr, "[semantic_diff_analyzer_test] C++20 stub. Use git/semantic_diff_analyzer with STL/nlohmann::json when available.\n");
    return 0;
}

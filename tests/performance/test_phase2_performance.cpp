/**
 * Phase2 performance tests — C++20 stub (Qt-free).
 * Original used QtTest/QtTest, QElapsedTimer, QTemporaryDir.
 */

#include <cstdio>
#include <cstdlib>

int main(int, char**) {
    std::fprintf(stderr, "[test_phase2_performance] C++20 stub. Use std::chrono and std::filesystem for perf tests.\n");
    return 0;
}
